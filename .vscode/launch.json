{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Open Beauty Parlour Website",
      "type": "chrome",
      "request": "launch",
      "file": "${workspaceFolder}/index.html",
      "webRoot": "${workspaceFolder}"
    }
  ]
}